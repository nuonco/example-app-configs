type           = "deprovision"
name           = "{{.nuon.install.id}}-deprovision"
description    = "Deprovision the sandbox and HTTPBin component."
display_name   = "deprovision role"
cloud_platform = "gcp"

[[policies]]
name                = "owner"
gcp_predefined_role = "roles/owner"
