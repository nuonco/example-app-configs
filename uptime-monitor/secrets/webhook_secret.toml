name         = "webhook_secret"
display_name = "Webhook Secret"
description  = "Secret key for signing and verifying webhook notifications when sites go down"
default      = "demo-webhook-secret-hmac-key"

user_configurable = true