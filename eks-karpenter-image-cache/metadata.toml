version = "v2"

description = "EKS with Karpenter nodes booting from a custom AMI carrying pre-cached container images and the gVisor runtime. Fast scale-out for large images, plus per-pod sandboxing via runtimeClassName."
display_name = "EKS Karpenter Image Cache + gVisor"
readme       = "./README.md"
