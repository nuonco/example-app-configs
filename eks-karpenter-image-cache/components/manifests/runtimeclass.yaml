apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: "{{.nuon.components.image_cache.outputs.runtime_handler}}"
scheduling:
  nodeSelector:
    nuon.co/node-pool: cached
  tolerations:
    - key: nuon.co/node-pool
      value: cached
      effect: NoSchedule
