# helm
name           = "prometheus"
type           = "helm_chart"
chart_name     = "prometheus"
namespace      = "grafana"
storage_driver = "configmap"
dependencies   = ["postgres_exporter"]
max_auto_retries = 5

[public_repo]
repo      = "prometheus-community/helm-charts"
directory = "charts/prometheus"
branch    = "main"

[[values_file]]
contents = "./values/prometheus/values.yaml"