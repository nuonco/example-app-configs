# kubernetes-manifest
name      = "temporal_ingress"
type      = "kubernetes_manifest"
namespace = "temporal"

dependencies = ["temporal"]
manifest = "./manifests/temporal_ingress.yaml"
max_auto_retries = 5
