type = "provision"
name = "{{.nuon.install.id}}-provision"
description = "provision the sandbox and components; trigger actions."
display_name = "provision role"

# Placeholder policy for Azure (actual permissions via RBAC in sandbox.tfvars)
[[policies]]
name = "placeholder"
contents = """
{
    "Version": "2012-10-17",
    "Statement": []
}
"""

