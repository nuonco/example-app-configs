name         = "forgejo_admin_password"
display_name  = "Forgejo Admin Password"
description   = "Initial administrator password (minimum 8 characters). Set at install time and used to create the admin on first boot."
required      = true

kubernetes_sync             = true
kubernetes_secret_namespace = "forgejo"
kubernetes_secret_name      = "forgejo-admin-password"
