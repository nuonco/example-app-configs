name         = "encryption_key"
display_name = "Encryption Key"
description  = "Encryption key for securing sensitive data in the Lambda function"
default      = "demo-encryption-key-256bit-secure"

user_configurable = true