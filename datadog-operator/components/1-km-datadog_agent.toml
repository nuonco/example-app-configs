# kubernetes-manifest
name      = "datadog_agent"
type      = "kubernetes_manifest"
namespace = "datadog"

dependencies = ["datadog_operator"]

manifest = "./manifests/datadog_agent.yaml"
max_auto_retries = 5
