# metadata

version = "v2"

display_name = "Langfuse"
description  = "Open-source LLM observability and tracing platform deployed full-plane in your AWS account."
readme       = "./README.md"

[onboarding]
enabled        = true
category       = "self-hosted"
difficulty     = "medium"
cloud_provider = "aws"
