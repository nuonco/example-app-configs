#:schema https://api.nuon.co/v1/general/config-schema?type=install

# install
# Mainline channel: opts into changes after canary validates them
# (install_groups order 2). Docs: https://docs.nuon.co/config-ref/install

name = "customer-1"

[labels]
mainline = "true"

# Valid options: 'prompt' (default, requires manual approval) or 'approve-all' (automatic approval)
approval_option = "prompt"

[aws_account]
region = "us-east-1"
