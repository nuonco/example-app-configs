#:schema https://api.nuon.co/v1/general/config-schema?type=install

# install
# Stable-train prod install after canary (install_groups order 2 on app branch stable).
# Docs: https://docs.nuon.co/config-ref/install

name = "customer-stable"
approval_option = "approve-all"

[labels]
prod    = "true"
cadence = "stable"

[aws_account]
region = "us-east-1"
