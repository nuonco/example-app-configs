#:schema https://api.nuon.co/v1/general/config-schema?type=install

# install
# Stable channel: gets the most vetted changes, after canary and mainline
# (install_groups order 3). Docs: https://docs.nuon.co/config-ref/install

name = "customer-2"
approval_option = "prompt"

[labels]
stable = "true"

[aws_account]
region = "us-east-1"

# required = true in inputs.toml — Nuon demands an explicit value even
# though a default exists; values below match those defaults.
[[inputs]]
cluster_version         = "1.34"
coder_db_instance_type  = "db.t4g.micro"
