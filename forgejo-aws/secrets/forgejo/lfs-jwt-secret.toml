name          = "forgejo_lfs_jwt_secret"
display_name  = "Forgejo LFS JWT Secret"
description   = "Signing key for Git LFS tokens. Auto-generated and kept stable across pods."
auto_generate = true

user_configurable = false

kubernetes_sync             = true
kubernetes_secret_namespace = "forgejo"
kubernetes_secret_name      = "forgejo-lfs-jwt"
