type                 = "deprovision"
name                 = "{{.nuon.install.id}}-deprovision"
description          = "deprovision role for ch-{{nuon.install.id}} install"
display_name         = "deprovision ch-{{nuon.install.id}}"
permissions_boundary = "{}"

[[policies]]
managed_policy_name = "AdministratorAccess"
