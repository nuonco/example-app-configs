# action
# copies and munges the autogenerated operator secret into a format the chart knows how to use
name    = "db_select_now"
timeout = "2m"

[[triggers]]
type = "manual"

[[steps]]
name            = "select now()"
inline_contents = "./src/db/select_now.sh"
