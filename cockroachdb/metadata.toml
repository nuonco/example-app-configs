version = "v2"

display_name = "CockroachDB"
description  = "CockroachDB on EKS. With support for Tailscale ingresses and (optional) role delegation."
readme       = "./README.md"

[onboarding]
enabled        = true
category       = "self-hosted"
difficulty     = "advanced"
cloud_provider = "aws"
