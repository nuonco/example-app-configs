name         = "external_api_token"
display_name = "External API Token"
description  = "Operator-provided token captured at install time and synced to a Kubernetes secret."
required     = true

kubernetes_sync             = true
kubernetes_secret_namespace = "secrets-demo"
kubernetes_secret_name      = "external-api-token"
